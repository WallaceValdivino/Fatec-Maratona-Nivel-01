#include <stdio.h>
 
int main() {
 
   int num, qtd;
   
   for(int i =0; i<5; i++){
       scanf("%d",&num);
       if(num % 2 == 0){
           qtd++;
       }
   }
 printf("%d valores pares\n", qtd);
    return 0;
}